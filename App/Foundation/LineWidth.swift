import CoreGraphics

/// The brief's one line weight for a drawn border: 1 px, whether on a
/// control (`line-control`), an info chip (`info-line`), the SOON badge
/// (`accent-quiet`), or the first-run card (`primary`).
enum LineWidth {
    static let border: CGFloat = 1
}
